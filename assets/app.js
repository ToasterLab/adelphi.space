// Generated by CoffeeScript 1.10.0
(function() {
  $(document).ready(function() {
    $('.parallax').parallax();
    return $.get("rooms.csv", function(data) {
      var attr, headers, i, j, k, len, len1, len2, ref, room, row, td, thebody;
      console.log(data);
      data = $.csv.toArrays(data);
      headers = $("<tr>");
      ref = data[0];
      for (i = 0, len = ref.length; i < len; i++) {
        td = ref[i];
        headers.append($("<th>").text(td));
      }
      data.splice(0, 1);
      thebody = $("<tbody>");
      for (j = 0, len1 = data.length; j < len1; j++) {
        room = data[j];
        row = $("<tr>");
        for (k = 0, len2 = room.length; k < len2; k++) {
          attr = room[k];
          row.append($("<td>").text(attr));
        }
        thebody.append(row);
      }
      return $("#details").append($("<table>").addClass("highlight").append($("<thead>").append(headers)).append(thebody));
    });
  });

}).call(this);

//# sourceMappingURL=app.js.map
